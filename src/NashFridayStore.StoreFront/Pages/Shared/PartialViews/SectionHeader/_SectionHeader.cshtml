@model SectionHeaderVM

<div class="text-center mb-16">
    <h2 class="text-4xl font-black uppercase tracking-widest text-base-content mb-4">@Model.Title</h2>
    <div class="w-24 h-1 bg-primary mx-auto mb-4"></div>
    <p class="text-base-content/60 tracking-wider">@Model.Description</p>
</div>